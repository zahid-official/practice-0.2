import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Product",
};

// ProductsPage Component
const ProductsPage = () => {
  return (
    <div>
      <h1>Welcome to the ProductsPage Component</h1>
    </div>
  );
};

export default ProductsPage;
